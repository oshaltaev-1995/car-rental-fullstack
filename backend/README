# Car Rental Backend

Python Flask API for handling car data and bookings.

## Run locally

```bash
cd backend
python3 -m venv venv
source venv/bin/activate   # или venv\Scripts\activate на Windows
pip install -r requirements.txt
python app.py
